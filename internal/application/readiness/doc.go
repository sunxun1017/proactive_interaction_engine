// Package readiness validates an explicitly selected capability deployment and
// determines whether a scenario can be activated or must use a declared safe
// fallback. It owns no provider lifecycle, I/O, or runtime registration state.
package readiness
