// Package engine orchestrates ingress, event compilation, projection, pure
// decision, planning, audit, and action dispatch through application ports.
// It never imports concrete adapters.
package engine
