// Package supervisor owns two local device process groups and exposes each
// explicitly authorized single-capability logical provider independently. It
// does not inspect media or application state.
package supervisor
