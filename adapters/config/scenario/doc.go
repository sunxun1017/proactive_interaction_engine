// Package scenario decodes versioned scenario manifests into validated,
// canonical application readiness requirements. It reads from an injected
// stream and does not select providers, inspect runtime policy, or own paths.
package scenario
