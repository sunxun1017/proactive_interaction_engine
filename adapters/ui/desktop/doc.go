// Package desktop implements the typed loopback desktop gRPC service. It maps
// immutable avatar, privacy, and provider-runtime state for the UI and routes
// explicit rejection only through the runtime P0 control boundary.
package desktop
