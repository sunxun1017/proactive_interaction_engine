// Package desktop implements the typed loopback desktop gRPC service. It maps
// immutable avatar and privacy state for the UI and routes explicit rejection
// only through the runtime P0 control boundary.
package desktop
