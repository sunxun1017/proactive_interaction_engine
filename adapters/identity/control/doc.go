// Package control implements the private local identity-worker desired-state
// boundary. Biometric template bytes exist only inside this adapter package and
// its generated transport messages; they never enter application or domain
// state.
package control
