// Package ingress maps strongly typed biometric evidence transport messages
// into metadata-only application candidates. It never accepts media,
// embeddings, templates, or trusted identity results from workers.
package ingress
