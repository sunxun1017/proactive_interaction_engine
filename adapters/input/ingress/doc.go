// Package ingress validates capability-provider observations at the gRPC
// boundary and submits canonical observations through the runtime single
// writer. It does not interpret behavior plans or mutate application state.
package ingress
